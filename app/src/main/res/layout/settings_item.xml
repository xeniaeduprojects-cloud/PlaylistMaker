<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
    <data>
        <import type="android.view.View" />
        <variable name="title"      type="String" />
        <variable name="icon"       type="android.graphics.drawable.Drawable" />
        <variable name="showSwitch" type="boolean" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="@color/yp_white">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="@dimen/settings_item_height"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingStart="@dimen/content_padding"
            android:paddingEnd="@dimen/content_padding"
            android:background="?attr/selectableItemBackground">

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:textSize="@dimen/settings_item_text_size"
                android:fontFamily="@font/ys_display_regular"
                android:textColor="@color/yp_black"
                android:text="@{title}"
                tools:text="Setting item" />

            <androidx.appcompat.widget.SwitchCompat
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:checked="true"
                android:layout_gravity="center_vertical"
                tools:visibility="visible"
                android:visibility="@{showSwitch ? View.VISIBLE : View.GONE}" />

            <ImageView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:contentDescription="@{title}"
                android:src="@{icon}"
                android:visibility="@{showSwitch ? View.GONE : View.VISIBLE}"
                app:tint="@color/yp_grey"
                tools:visibility="gone"
                tools:src="@drawable/ic_arrow_forward" />

        </LinearLayout>
    </LinearLayout>

</layout>