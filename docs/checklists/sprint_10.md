- [x] После рефакторинга приложение выглядит и работает в соответствии с дизайном и установленными ранее функциональными требованиями.
- [x] Значения атрибутов для вёрстки вынесены в ресурсные файлы.
- [x] Созданы классы, описывающие элемент списка, `ViewHolder` и `Adapter`.
- [x] В классе `ViewHolder` есть метод `bind()`, в который передаётся объект типа `Track`.
- [x] `Inflate` выполняется в конструкторе `ViewHolder`-класса.
  - ???: почему viewholder а не adapter? убрала во viewholder оедльным коммитом, не понимаю почему.
- [x] Для загрузки обложки трека используется Glide, подключённый к проекту в виде библиотеки.
- [x] Список и его элементы соответствуют дизайну (корректные цвета, отступы, шрифт, размер и цвет текста, длинные строки обрезаются точками и не сдвигают другие элементы).
- [x] Углы обложек треков закруглены.
- [x] При переключении телефона в ночной режим список отображается в соответствии с дизайном для тёмной темы.
- [x] Если на устройстве нет интернета, то вместо изображения обложки показывается плейсхолдер.

----

- [x] Не забудьте добавить в манифест запись о разрешении на использование интернета.
- [x] Чтобы использовать Glide внутри `ViewHolder`, передавайте в метод `with()` объект `itemView`.
- [x] Все изображения обложек квадратные, но могут оказаться слишком маленькими для некоторых устройств.
- [x] Используйте тип масштабирования `fitCenter` или `centerCrop`.