# Чек-лист: рефакторинг приложения в Clean Architecture

## 1. Разделение на слои
- [x] Создать слой **Data** — классы для доступа к данным (сетевые клиенты, SharedPreferences)
- [x] Создать слой **Domain** — бизнес-логика и доменные сущности (интерфейсы репозиториев, интеракторы)
- [x] Создать слой **Presentation** — классы для отображения данных (Activity, адаптеры)
- [x] Убедиться, что зависимости направлены внутрь: Presentation → Domain ← Data

## 2. Создание DTO
- [x] Выделить сущности, передаваемые между слоями Data и Domain
- [x] Создать DTO-классы для этих сущностей
- [x] Реализовать маппинг DTO ↔ доменные модели (mapper-классы или функции)
- [x] Проверить, что Domain не зависит от DTO слоя Data напрямую

## 3. Репозитории
- [x] Создать интерфейсы репозиториев в слое ~~**Domain**~~ Data (by design)
- [x] Реализовать репозитории в слое **Data**
- [x] Обеспечить доступ к данным через сетевые клиенты и SharedPreferences внутри реализаций
- [x] Скрыть детали источников данных за интерфейсом репозитория

## 4. Интеракторы
- [ ] Создать интерфейсы интеракторов для каждой значимой операции - не понимаю зачем интерфейсы для usecases
- [x] Реализовать классы интеракторов
- [x] Обеспечить взаимодействие интеракторов с репозиториями
- [x] Разместить бизнес-логику внутри интеракторов

## 5. Организация по пакетам
- [x] Создать пакет `data` — слой Data
- [x] Создать пакет `domain` — слой Domain
- [x] Создать пакет `ui` — слой Presentation
- [x] Распределить все исходные файлы по соответствующим пакетам
- [x] Внутри пакетов при необходимости выделить подпакеты (`api`, `dto`, `repository`, `models`, `interactor` и т. д.)

## 6. Creator-класс - DI
- [x] Koin выполняет функцию creator

## 7. Взаимодействие с UI
- [x] Организовать взаимодействие UI со слоем Domain через интеракторы напрямую из ~~Activity~~ VM
- [ ] ~~Создавать экземпляры интеракторов через Creator-класс~~ Koin
- [x] Убедиться, что Activity не обращается к слою Data напрямую
- [x] Проверить, что Presentation работает только с доменными моделями (не с DTO)

## Финальная проверка
- [x] Приложение собирается и запускается без ошибок
- [x] Каждый слой отвечает только за свою зону ответственности
- [x] Зависимости между слоями соответствуют принципам Android Clean Architecture - by design
